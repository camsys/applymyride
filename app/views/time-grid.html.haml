.row
  %div{ng: {repeat: 'time in times'}}
    .col-lg-1.col-xs-3.time.text-center{ng: {click: 'select(time)', class: '{ selected: time.isSelected }'}}
      {{ time.display }}

.row
  %div{ng: {repeat: 'mer in meridians'}}
    .col-xs-1.col-xs-3.time.text-center{ng: {click: 'selectMeridian(mer)', class: '{ selected: mer.isSelected }'}}
      {{ mer.display }}
